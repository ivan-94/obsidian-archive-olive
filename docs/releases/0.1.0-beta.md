# Archive Olive 0.1.0 Beta Release Record

| Field               | Value                                                                |
| ------------------- | -------------------------------------------------------------------- |
| Status              | Published; post-release acceptance pending                           |
| Published tag       | `0.1.0`                                                              |
| Release type        | GitHub pre-release                                                   |
| Channel             | BRAT follows `main/theme.css`                                        |
| Compatibility floor | Obsidian `1.12.7`                                                    |
| Prepared            | 2026-07-25                                                           |
| Release URL         | https://github.com/ivan-94/obsidian-archive-olive/releases/tag/0.1.0 |

The owner authorized and published an opt-in GitHub pre-release before completing the human pilot. Pending acceptance remains visible and still blocks broader announcement and official-directory submission.

## Candidate release notes

The exact public-facing body is maintained in [`0.1.0-beta-notes.md`](0.1.0-beta-notes.md). Keep release prose there so the handoff checklist and published notes cannot drift as duplicate copies.

## Required release assets

All assets must be taken from the exact tagged commit:

| Asset                                          | Required check                                                              |
| ---------------------------------------------- | --------------------------------------------------------------------------- |
| `theme.css`                                    | Contains `Archive Olive 0.1.0` and the `main/theme.css` BRAT channel marker |
| `manifest.json`                                | Valid JSON; version and tag both equal `0.1.0`                              |
| `LICENSE`                                      | MIT license                                                                 |
| `assets/screenshots/archive-olive-512x288.png` | Public-safe `512 × 288` release image                                       |

Record SHA-256 checksums before upload. Never replace assets on an existing published tag; create a new patch version instead.

## Publication checklist

- [ ] Three to five post-release private-pilot environments are recorded.
- [x] A clean vault fresh-installed through BRAT during the macOS lifecycle smoke test.
- [x] BRAT delivered a changed `theme.css` during the macOS lifecycle smoke test.
- [x] Both Issue Forms render in a signed-in GitHub session with the intended fields.
- [ ] A post-release tester submits a non-sensitive dry-run report through an Issue Form.
- [x] No unresolved issue or blocker remains at the pre-release decision.
- [x] Known limitations are listed in README and the public release notes.
- [x] Owner authorizes pre-release publication with visual acceptance pending.
- [ ] Owner completes the post-release BRAT visual go/no-go.
- [x] `node scripts/validate.mjs --release` passes on the release commit.
- [x] GitHub Actions is green on release commit `034515d`.
- [x] `git status --short` contained no uncommitted release-owned change.
- [x] The official community theme index was rechecked for the `Archive Olive` name on 2026-07-25.
- [x] Tag `0.1.0` is absent before the release operation.
- [x] The annotated `0.1.0` tag resolves to release commit `034515d` and is pushed without rewriting history.
- [x] `theme.css`, `manifest.json`, `LICENSE`, and the release image are attached from that tagged commit.
- [x] The GitHub release is published and marked as a pre-release.

## Publication commands

Run after the release commit passes validation and replace `<approved-commit>` with the reviewed full commit SHA:

```sh
git fetch origin
test "$(git rev-parse HEAD)" = "<approved-commit>"
git diff --quiet -- theme.css manifest.json LICENSE assets/screenshots/archive-olive-512x288.png docs/releases/0.1.0-beta-notes.md
git tag -a 0.1.0 <approved-commit> -m "Archive Olive 0.1.0 beta"
git push origin 0.1.0
gh release create 0.1.0 \
  theme.css \
  manifest.json \
  LICENSE \
  assets/screenshots/archive-olive-512x288.png \
  --repo ivan-94/obsidian-archive-olive \
  --verify-tag \
  --prerelease \
  --title "Archive Olive 0.1.0 Beta" \
  --notes-file docs/releases/0.1.0-beta-notes.md
```

After publication, read back the tag target, release flags, notes, and asset checksums. If publication fails after the tag is pushed, leave the tag intact, diagnose the release, and do not force-move the tag.

## Rollback

BRAT follows `main`, not the pre-release asset. Revert a faulty change with a new commit that intentionally changes `theme.css`, run the complete validation workflow, and verify the restored stylesheet through BRAT. Do not rewrite `main` or the published tag.

## Source Manifest

### Sources

- User direction in this Codex thread: prepare a BRAT-first release and leave real cross-platform acceptance to the owner.
- [BRAT Beta release specification](../specs/brat-beta-release.md).
- [Cross-platform HAT guide](../../hats/20260725-brat-beta-cross-platform/guide.md).
- [README](../../README.md).
- [Changelog](../../CHANGELOG.md).
- [Validation evidence](../../VALIDATION.md).
- [Manifest](../../manifest.json).
- [GitHub CLI release documentation](https://cli.github.com/manual/gh_release_create).

### Produced artifacts

- `docs/releases/0.1.0-beta.md`
- `docs/releases/0.1.0-beta-notes.md`
- [Archive Olive 0.1.0 Beta pre-release](https://github.com/ivan-94/obsidian-archive-olive/releases/tag/0.1.0)

### Key decisions

- Publish an opt-in pre-release now by owner direction and complete human BRAT acceptance afterward.
- Use an annotated tag exactly matching manifest version `0.1.0`.
- Attach assets from the immutable tagged commit and use a new patch version for any published correction.

### Verification evidence

- Baseline repository validation and GitHub Actions pass.
- macOS BRAT install and changed stylesheet delivery pass.
- Both Issue Forms render in a signed-in Chrome session with their intended fields, required markers, privacy check, and Create control; no issue was submitted during this read-only check.
- The official community theme index had 647 entries and no Archive Olive name or repository match when rechecked on 2026-07-25.
- The repository had no open issue when checked immediately before publication.
- The publication commands use `--verify-tag` and `--prerelease`.
- GitHub Actions run [30148368218](https://github.com/ivan-94/obsidian-archive-olive/actions/runs/30148368218) passed on release commit `034515ddc03b245d2635324465f8aac8e47bea43`.
- Annotated tag `0.1.0` dereferences to `034515ddc03b245d2635324465f8aac8e47bea43`.
- The published release is not a draft and is marked as a pre-release.
- Downloaded asset SHA-256 values match the tagged files: `theme.css` `dd5323d8…`, `manifest.json` `42c6912a…`, `LICENSE` `ba2c1e00…`, and release image `94f0512b…`.
- The published release body matches `docs/releases/0.1.0-beta-notes.md`.

### Open questions / risks

- A private-pilot Issue Form submission and real Windows, Linux, iOS, and Android acceptance remain external.
- Owner-led BRAT visual acceptance is deferred until after pre-release publication.
- GitHub Raw caching can delay BRAT rollback delivery.
