# Archive Olive agent rules

## Obsidian acceptance

- Use the repository's isolated `test-vault/` for all Obsidian runtime acceptance, visual review, screenshots, and interactive testing.
- Do not use or modify a personal vault for theme acceptance.
- Keep `test-vault/.obsidian/themes/Archive Olive/theme.css` and `manifest.json` linked to the repository root so runtime checks exercise the current working tree.

## Source Manifest

- User direction in the Codex task that reported low-contrast tooltips and notices: use `test-vault/` for acceptance and persist that rule in `AGENTS.md`.
- Repository fixture and live-theme links under `test-vault/.obsidian/themes/Archive Olive/`.
